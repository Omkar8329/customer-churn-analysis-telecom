# Assets

Generated figures such as `churn_count.png` are written here when the Python analysis script runs. Generated images are ignored by Git by default to keep the repository lightweight.

The interactive dashboard is maintained in Tableau and linked from the root README.
